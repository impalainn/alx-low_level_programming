#include "main.h"
/**
 * is_palindrome - checks palindrome string
 * @s; string
 * Return: 1 if palindrome, otherwise 0
 */
int is_palindrome(char *s)
{
	int len = strlen(s);

	if (*s == " " || len == 1)
		return (1);
	else if 

