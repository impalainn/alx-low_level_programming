#include<stdio.h>
/**
 * main - prints number of arguements passed
 * @argc: number of args
 * @argv: array of strings
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", (argc - 1));
	return (0);
}
